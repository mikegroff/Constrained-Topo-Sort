All text files are now in inputs file. So normal testcases 1-5 will not work.
The testcases in this folder are modified to grab from that inputs file.
There are pictures of the graphs if anyone needs to trace through them.

Logic has been modified such that if no valid toposort exisits, program should fail.